# Database Configuration
DB_USER=hexasend
DB_PASSWORD=secure_password123
DB_NAME=hexasend

# Application Configuration
NODE_ENV=production
PORT=5000

# Database URL (automatically constructed in docker-compose.yml)
DATABASE_URL=postgresql://hexasend:secure_password123@postgres:5432/hexasend

# Optional: Add any other environment variables your app needs
# SESSION_SECRET=your_session_secret_here